// Shared constants with no Leaflet/DOM dependency (so pure modules that need
// them — e.g. coords.js — stay importable in tests without a browser).
export const IMG = 2048; // xivapi map images are 2048x2048
