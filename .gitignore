node_modules/
*.log
.DS_Store
.state.json
custom-markers.json
overlay-config.json
scripts/.tc-cache/

# Test coverage reports
coverage/

# esbuild web bundle (built from public/src + styles.css)
public/dist/

# electron-builder artifacts (the .dmg + the unpacked .app)
release/
# transient icon scratch (icon.icns + icon-1024.png are committed)
build/icon.iconset/

# Local Claude agent config + memory (personal notes — never share)
.claude/

# Derived data — built on first run from Teamcraft GitHub + XIVAPI v2
data/
